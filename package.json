{
  "name": "spatial-audio-new",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "npm run test:audio && npm run test:e2e",
    "test:audio": "python -m unittest discover -s tests -p test_*.py",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
